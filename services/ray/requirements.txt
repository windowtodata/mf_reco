# Training Pipeline Dependencies (Host Machine)
# Python 3.12 compatible

# Core ML frameworks
torch>=2.0.0
ray[train,serve]==2.51.0

# Data processing - Choose ONE option:

# Option 1: Pandas (Recommended for 250MB data + Python 3.12)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Option 2: PySpark + RayDP (Only if you need distributed processing)
# Uncomment if you want to use PySpark instead of pandas:
raydp==1.7.0b20250423.dev0
pyspark>=3.4.0,<3.4.3

# Monitoring and tracking (Host-side MLflow client)
mlflow>=2.9.0
psycopg2-binary>=2.9.0
boto3>=1.28.0
protobuf>=3.19.4
# Note: Full MLflow server runs in Docker with additional dependencies
# See requirements-mlflow.txt for Docker container dependencies

# InfluxDB client (optional)
influxdb-client>=1.38.0

# Utilities
configparser

